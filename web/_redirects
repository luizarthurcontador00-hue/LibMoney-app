# Sem regras por enquanto: só há páginas estáticas aqui.
#
# Quando o app voltar para cá, esta linha volta junto — ela faz qualquer
# caminho dentro de /app cair no index dele, que resolve as rotas sozinho:
#
# /app/*  /app/index.html  200
